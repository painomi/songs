-# frozen_string_literal: true
= simple_form_for(@person) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :description

  .form-actions
    = f.button :submit, class: 'btn btn-raised btn-primary'
